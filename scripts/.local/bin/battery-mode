#!/bin/bash

if [ "$1" == "80" ]; then
    sudo tlp setcharge 75 80
    echo "Battery limited to 75-80%"
elif [ "$1" == "100" ]; then
    sudo tlp setcharge 0 100
    echo "Battery set to 100%"
else
    echo "Usage: battery-mode 80|100"
fi
